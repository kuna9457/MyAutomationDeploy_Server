# Core runtime. The UI is the React app in frontend/ — this process serves the
# API only, so no Streamlit/plotly here (removed; charting is Recharts client-side).
pandas>=2.0
numpy>=1.24
pymongo>=4.5
openpyxl>=3.1          # Excel export in db_manager.export_*
python-dotenv>=1.0
requests>=2.31
# FastAPI backend layer (api/) — this is the process you actually run:
#   uvicorn api.main:app --host 127.0.0.1 --port 8000
fastapi>=0.110
uvicorn[standard]>=0.29
pyjwt>=2.8
python-multipart>=0.0.9
# Encryption at rest for per-client broker credentials (credentials_vault.py).
# Usually already present as a pymongo dependency; pinned explicitly so it is
# never relied on transitively.
cryptography>=42.0
# Optional broker SDKs — install only the ones you actually use.
# The bot runs fully in Paper/Simulation mode without any of these.
upstox-python-sdk>=2.9
# dhanhq>=2.0
kiteconnect>=5.0  # Zerodha
# neo-api-client   # Kotak Neo (from their GitHub)
# yfinance>=0.2    # optional: real historical data for the backtester
